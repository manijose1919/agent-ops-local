# AI Agent Task Telemetry & Cost Analyzer
# Environment Variables

# Server Configuration
PORT=8000
HOST=0.0.0.0

# Database (Leave blank to use default SQLite database local.db)
# DATABASE_URL=postgresql://user:password@localhost:5432/agent_ops

# Authentication (Optional for securing the ingest API)
# API_SECRET_KEY=generate_a_random_secure_string

# Frontend
VITE_API_BASE_URL=http://localhost:8000
